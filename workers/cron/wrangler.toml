name = "family-app-cron"
main = "src/index.ts"
compatibility_date = "2024-09-23"

[triggers]
crons = ["0 12 * * 6"]  # Saturday 12:00 UTC = 13:00 CET

# APP_URL and CRON_SECRET set via `wrangler secret put`
